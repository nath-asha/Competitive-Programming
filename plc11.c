//balloon seller problem
#include<stdio.h>
int main()
{    int n;
    char word[n],gets(word);
    int a[26]={0};
    printf("No of balloons");
    scanf("%d",&n);
    for(int i=0;i<strlen(word);i++)
    {
        a[word[i]-97]++;
    }
    for(int i=0;i<strlen(word);i++){
      
    }
}